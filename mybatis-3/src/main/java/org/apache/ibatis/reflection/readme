1.反射功能描述
java中的反射功能虽然强大，但是对于大多数的开发人员来说，写出高质量的反射代码还是有一定难度的。Mybatis中专门提供了反射模块，改模块对Java原生的反射
进行了良好的封装，提了更加简洁易用的API，方便上层调用，并且对反射操作进行了一系列的优化，例如缓存了类的元数据，提供了反射操作的功能。
2.reflection是基础支持层，可以断点来调试代码

3.java8替代java7的一个写法
  List<Method> list = conflictingMethods.computeIfAbsent(name, k -> new ArrayList<>());
        list.add(method);

        //java7写法
//    List<Method> list = conflictingMethods.get(name);
//    if (list == null) {
//      list = new ArrayList<Method>();
//      conflictingMethods.put(name, list);
//    }
//    list.add(method);

4.将数组变成java8流的方式
Arrays.stream(methods)


5.isAssignableFrom()方法与instanceof关键字的区别总结为以下两个点：
  a.isAssignableFrom()方法是从类继承的角度去判断，instanceof关键字是从实例继承的角度去判断。
  b.isAssignableFrom()方法是判断是否为某个类的父类，instanceof关键字是判断是否某个类的子类。
  使用方法
  父类.class.isAssignableFrom(子类.class)
  子类实例 instanceof 父类类型